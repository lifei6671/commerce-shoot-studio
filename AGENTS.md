# 商拍工坊 Agent 工作规范

本文件记录 `commerce-shoot-studio` 的项目级硬规则。后续任何 Agent 修改
`desktop/`、`docs/` 或 `license-server/` 时，必须先阅读并遵守本文件。

## 强制交互规则

1. 所有用户可见的操作消息必须使用统一 toast 展示。
   - 包括创建、保存、删除、导入、刷新、校验、执行、取消、失败、成功、警告和信息提示。
   - 禁止在表单、卡片、属性面板、设置页、弹窗正文或画布节点内新增横幅式消息提示。
   - 禁止用 `promptTemplateMessage` 这类页面内成功提示条替代 toast。
   - toast 必须自动消失；持续不消失的 toast 只允许用于真实进行中的后台任务，并且任务结束后必须关闭或更新。
   - 字段级校验可以保留在字段附近，但它只能描述当前字段或当前表单的输入错误，不能承载操作结果消息。

2. 所有按钮和菜单必须是真实交互。
   - 禁止保留假按钮、假下载、假刷新、假保存、假导入和静态占位操作。
   - 暂未实现的能力必须显式禁用，并给出清晰标题或说明。

3. 设置页、方案中心、工作台面板的滚动区域必须彼此隔离。
   - 左侧列表和右侧编辑区各自滚动，不能共用页面级滚动。
   - 弹窗 footer 固定在底部，正文内部滚动，不能让内容挤压按钮区。

## Prompt 模板与输出方案规则

1. 对外统一使用“输出方案 / 方案中心 / Prompt 模板配置”术语。
   - 工作台普通用户不直接面对 `System Prompt / User Prompt / Negative Prompt` 三段内部概念。
   - 内部仍可保存 system/user/negative 模板，但工作台入口必须通过输出方案组织。

2. System 模板是内部规则。
   - 工作台输出方案编辑区不展示 system prompt 的可编辑模块。
   - 最终输出预览必须展示 system 结果。
   - system 不允许追加或替换，只能由方案绑定的模板决定。

3. 细节描述和避坑描述是用户可编辑的描述段。
   - user prompt 对外叫“细节描述”。
   - negative prompt 对外叫“避坑描述”。
   - 追加或替换模式出现时，必须出现输入框让用户输入内容。
   - 最终输出必须按方案中的模板进行替换或追加，不能只改展示层。

4. Prompt 模板变量必须从模板内容自动识别。
   - 变量来源是 `{{变量名}}`，不能让用户手工新增或删除参数名。
   - 参数区只能配置控件类型、选项值和默认值。
   - 内置模板变量默认使用“可选可输入”控件。
   - 变量默认值、方案中心参数区、工作台输出方案编辑区和最终输出预览必须共享同一份变量契约。

5. 新建 Prompt 模板只填写基本信息。
   - 弹窗只收集模板名称和模板类型。
   - 创建后关闭弹窗并选中新模板。
   - 模板内容、参数配置和预览必须在右侧编辑区继续完成。

6. 方案中心规则。
   - built-in 方案只读，不能编辑或删除。
   - 新建方案必须落库，后续可以重新加载。
   - 绑定输出方案模板时不展示“修改模式”。
   - 变量默认值只显示参数和值，不显示字段类型，不允许删除或重命名参数。

## 工作台与组合数据规则

1. 组合数据必须隔离。
   - 资源库人物、服装和结果都属于当前组合。
   - 切换组合时必须重新加载当前组合的人物、服装、结果和输出方案绑定。
   - 禁止让上一个组合的资源或绑定泄漏到当前组合。

2. 新建组合不默认填充图片。
   - 新建组合弹窗中的人物和服装必须为空。
   - 只能使用用户显式选择或导入的图片。

3. 工作台必须自动保存。
   - 导入图片、修改组合信息、修改输出方案、修改模型参数等工作台信息变化都要进入自动保存链路。
   - 自动保存必须写当前组合，不能使用旧 combination id。

4. 画布节点必须保持清晰。
   - 高倍率缩放时节点文字和图片不能模糊。
   - React Flow 选中、focus、resize 产生的黑边或默认 outline 必须主动收口。
   - 放大/最大化按钮的行为必须符合用户看到的 UI 语义，不能只做局部 viewport 操作。

5. 属性面板规则。
   - 人物节点属性面板只保留详情。
   - 所有节点属性面板去掉关联和历史。
   - 属性面板折叠态图标和文字不能重叠。

## 验证规则

1. 修改前端后至少运行：
   - `npm --prefix desktop run test`
   - `npm --prefix desktop run build`

2. 修改 Tauri/Rust 后至少运行：
   - `make cargo-check`
   - 相关 Rust 单测；新增测试时必须单独跑新增用例。

3. 修改 Prompt、输出方案、组合切换或资源库逻辑时，必须额外验证：
   - 切换组合后资源库和画布节点图片正确刷新。
   - 输出方案变量在设置页、方案中心、工作台属性区和最终预览同步。
   - 操作消息全部通过 toast 出现，页面内没有新增横幅提示。

4. 修改桌面 UI 后，能运行本地 app 时应回到 `商拍工坊` 做截图级视觉复核。
   - 重点看文字是否溢出、滚动是否串联、按钮是否换行、弹窗 footer 是否被挤压。
